/*
Explique a diferença entre os operadores && e || em JavaScript.

 o operador && exige que todas as condições sejam verdadeiras para que o resultado seja verdadeiro, enquanto o operador || requer que pelo menos uma condição seja verdadeira para que o resultado seja verdadeiro. Além disso, ambos os operadores suportam a lógica de curto-circuito, o que significa que a avaliação para de imediato assim que o resultado final for determinado.
*/